[metadata]
name = scmaine-tokens
version = 0.1.0

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.9
install_requires =
    httpx==0.28.1

[options.packages.find]
where = src

[options.extras_require]
test =
    pytest==8.4.2
    pytest-mock==3.15.1
